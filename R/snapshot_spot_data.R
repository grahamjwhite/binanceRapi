# Binance Public Spot API
#
# OpenAPI Specifications for the Binance Public Spot API  API documents:   - [https://github.com/binance/binance-spot-api-docs](https://github.com/binance/binance-spot-api-docs)   - [https://binance-docs.github.io/apidocs/spot/en](https://binance-docs.github.io/apidocs/spot/en)
#
# The version of the OpenAPI document: 1.0
# 
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title SnapshotSpotData
#'
#' @description SnapshotSpotData Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field balances  list( \link{SnapshotSpotDataBalances} ) 
#'
#' @field totalAssetOfBtc  character 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
SnapshotSpotData <- R6::R6Class(
  'SnapshotSpotData',
  public = list(
    `balances` = NULL,
    `totalAssetOfBtc` = NULL,
    initialize = function(
        `balances`, `totalAssetOfBtc`, ...
    ) {
      local.optional.var <- list(...)
      if (!missing(`balances`)) {
        stopifnot(is.vector(`balances`), length(`balances`) != 0)
        sapply(`balances`, function(x) stopifnot(R6::is.R6(x)))
        self$`balances` <- `balances`
      }
      if (!missing(`totalAssetOfBtc`)) {
        stopifnot(is.character(`totalAssetOfBtc`), length(`totalAssetOfBtc`) == 1)
        self$`totalAssetOfBtc` <- `totalAssetOfBtc`
      }
    },
    toJSON = function() {
      SnapshotSpotDataObject <- list()
      if (!is.null(self$`balances`)) {
        SnapshotSpotDataObject[['balances']] <-
          lapply(self$`balances`, function(x) x$toJSON())
      }
      if (!is.null(self$`totalAssetOfBtc`)) {
        SnapshotSpotDataObject[['totalAssetOfBtc']] <-
          self$`totalAssetOfBtc`
      }

      SnapshotSpotDataObject
    },
    fromJSON = function(SnapshotSpotDataJson) {
      SnapshotSpotDataObject <- jsonlite::fromJSON(SnapshotSpotDataJson)
      if (!is.null(SnapshotSpotDataObject$`balances`)) {
        self$`balances` <- ApiClient$new()$deserializeObj(SnapshotSpotDataObject$`balances`, "array[SnapshotSpotDataBalances]", loadNamespace("binanceRapi"))
      }
      if (!is.null(SnapshotSpotDataObject$`totalAssetOfBtc`)) {
        self$`totalAssetOfBtc` <- SnapshotSpotDataObject$`totalAssetOfBtc`
      }
      self
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`balances`)) {
        sprintf(
        '"balances":
        [%s]
',
        paste(sapply(self$`balances`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )},
        if (!is.null(self$`totalAssetOfBtc`)) {
        sprintf(
        '"totalAssetOfBtc":
          "%s"
                ',
        self$`totalAssetOfBtc`
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(SnapshotSpotDataJson) {
      SnapshotSpotDataObject <- jsonlite::fromJSON(SnapshotSpotDataJson)
      self$`balances` <- ApiClient$new()$deserializeObj(SnapshotSpotDataObject$`balances`, "array[SnapshotSpotDataBalances]", loadNamespace("binanceRapi"))
      self$`totalAssetOfBtc` <- SnapshotSpotDataObject$`totalAssetOfBtc`
      self
    }
  )
)
